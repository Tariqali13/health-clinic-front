import React from 'react'
import PropTypes from 'prop-types'
import AppointmentsBody from '../../components/appointmnets/AppointmentsBody'

const Appointments = props => {
    return (
        <AppointmentsBody/>
    )
}

Appointments.propTypes = {

}

export default Appointments
